describe('user-service', () => {
  it('should respond ok when login an user', () => {
    // TODO: pending

    expect(false).toStrictEqual(true);
  });

  it('should respond with bad request when trying to login an nonexisting user', () => {
    // TODO: pending

    expect(false).toStrictEqual(true);
  });

  it('should respond ok when sign up a new user', () => {
    // TODO: pending

    expect(false).toStrictEqual(true);
  });

  it('should respond with bad request when trying to sign up an existing user', () => {
    // TODO: pending

    expect(false).toStrictEqual(true);
  });
});
